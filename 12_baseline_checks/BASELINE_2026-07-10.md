# Baseline ERC/DRC — commit `ad580da`, 2026-07-10

Real, measured baseline for comparison against a subsequent local Codex CLI
pass. Generated with `kicad-cli` 9.0.8 against the exact files this
Codex/Claude Code prompt targets.

## Commands run

```bash
kicad-cli sch erc \
  07_engineering_reconcile/00_OPEN_IN_KICAD_PATCHED/TFLN_AI_NODE_X2_SERVER_CHASSIS_IO.kicad_sch \
  --format json --output 12_baseline_checks/erc_report_2026-07-10.json

kicad-cli pcb drc \
  07_engineering_reconcile/00_OPEN_IN_KICAD_PATCHED/TFLN_AI_NODE_X2_SERVER_CHASSIS_IO.kicad_pcb \
  --format json --output 12_baseline_checks/drc_report_2026-07-10.json
```

## Results

| Check | Total | Breakdown |
|---|---|---|
| ERC | 164 | pin_not_connected 112, endpoint_off_grid 21, lib_symbol_issues 13, unconnected_wire_endpoint 12, wire_dangling 6 |
| DRC | 82 | lib_footprint_mismatch 82 (all), unconnected_items 0, schematic_parity 0 |

Raw reports: `erc_report_2026-07-10.json`, `drc_report_2026-07-10.json`.

## Note on schematic_parity

This kicad-cli invocation returns an empty `schematic_parity` list. The
repo's earlier documentation (`07_engineering_reconcile/00_OPEN_IN_KICAD_PATCHED/after_non_netlist_fab_readiness_drc.json`)
previously cited 214 schematic-parity warnings — that figure has not been
reproduced by this command in this environment/version and remains
unreconciled. If your local run shows a nonzero schematic_parity count,
that's useful signal on its own and worth flagging back.

## How to compare after your local Codex run

Run the same two commands from repo root (see
`11_production_fab_roadmap/CODEX_PROMPT_LOCK_LAYOUT.md` for the prompt used)
after Codex finishes, and diff your totals/breakdown against the table
above. Push your `erc_report_*.json` / `drc_report_*.json` alongside this
folder so there's a real, comparable record — not just a claimed number.
