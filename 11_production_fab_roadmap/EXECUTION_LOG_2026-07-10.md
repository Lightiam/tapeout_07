# Execution Log — 2026-07-10

Executes the bounded first pass of `CODEX_PROMPT_LOCK_LAYOUT.md` against the
real working files. This was run directly against the local clone (two
automated attempts via a delegated coding subagent both failed with an
infrastructure-level API serialization error, unrelated to the task content
— logged internally; the work below was completed directly instead).

## Files changed

1. **`09_pin_complete_data/NCE_BGA2500_DRAFT_BALL_MAP.csv`** (new, 2500 rows)
   — systematic DRAFT ball-out for the NCE Gen3 BGA-2500 die (U1/U4),
   generated by `99_generation_scripts/generate_nce_draft_ball_map.py`.
   Every row tagged `DRAFT-PENDING-IP-OWNER-SIGNOFF`. Zone breakdown:
   HBM_CHANNEL 667 (26.7%), NC_RESERVED 609 (24.4%), OPTICAL_SERDES 352
   (14.1%), PCIE_FABRIC 308 (12.3%), POWER 286 (11.4%), GROUND 188 (7.5%),
   CTRL 90 (3.6%).
2. **`09_pin_complete_data/NCE_BGA2500_DRAFT_BALL_MAP_README.md`** (new) —
   explains what the ball map is/is not and restates the signoff rule.
3. **`99_generation_scripts/generate_nce_draft_ball_map.py`** (new) —
   the generator script (kept in-repo so the allocation logic is
   inspectable/re-runnable, not just the output).
4. **`00_step01_gerber_bom_only/TFLN_AI_NODE_X2_STEP_01_BOM.xlsx`**
   (modified, "Grouped BOM" sheet, rows 5-7 only):
   - Row 5 (U3, TFLN PIC): MPN column now documents HyperLight as primary
     sourcing target (fallback Ligentec/CSEM/POET Technologies); explicitly
     flags the existing `TFLN-MZM-400G-C` value as a non-orderable
     placeholder. No pad map invented.
   - Row 6 (U30-U35, U40-U45, HBM4): MPN column now names a real, currently
     listed part — **Samsung HBM3E Shinebolt `KHBBC4B03B-MC1J`** (8.6Gbps) —
     as the SUBSTITUTE PENDING HBM4 ALLOCATION, replacing the placeholder
     `SK_HBM4_16GB_12H` (confirmed not a real MPN). Package/footprint
     co-design against the current `BGA-1024_12x9mm` placeholder footprint
     is flagged as unverified and NOT changed.
   - Row 7 (U50-U53, HMC8410): MPN column now confirms the mismatch (real
     HMC8410LP2FE is a 0.01-10GHz LNA in a 6-lead 2mm x 2mm LFCSP, not a
     100GHz driver in QFN-32 5x5mm as the schematic's Description field
     claimed) and recommends a real replacement — **Renesas HXT45430**
     (112Gb/s PAM4 Silicon Photonic Modulator Driver). Footprint/package
     co-design against the current QFN-32-1EP 5x5mm placeholder is flagged
     as unverified and NOT changed.
5. **`07_engineering_reconcile/00_OPEN_IN_KICAD_PATCHED/TFLN_AI_NODE_X2_SERVER_CHASSIS_IO.kicad_sch`**
   (modified, U50/U51/U52/U53 only): added a new hidden
   `Substitute_Recommendation` property to each of the 4 HMC8410 symbol
   instances, stating the mismatch and the recommended Renesas HXT45430
   replacement, and explicitly noting that the footprint/placement were
   **not** touched. No symbol library id, footprint reference, coordinates,
   or any other geometry/topology changed — layout stays byte-identical
   otherwise. The corresponding `.kicad_pcb` was left untouched entirely
   (no PCB-level property edits made, to avoid uuid-collision risk in that
   file's more verbose property block format).

## Real before/after ERC/DRC numbers

Both measured with `kicad-cli` 9.0.8 against the same files.

| Check | Before | After | Changed? |
|---|---|---|---|
| ERC violations (`kicad-cli sch erc`) | 164 (pin_not_connected 112, endpoint_off_grid 21, lib_symbol_issues 13, unconnected_wire_endpoint 12, wire_dangling 6) | 164 (identical breakdown) | **No** |
| DRC violations (`kicad-cli pcb drc`) | 82, all `lib_footprint_mismatch` | 82, all `lib_footprint_mismatch` | **No** |
| DRC unconnected items | 0 | 0 | No |
| DRC schematic_parity | 0 (this field returns empty in this kicad-cli version/invocation — does not reproduce the repo's previously-documented "214 schematic-parity warnings"; still unreconciled, needs a different report mode or a netlist-diff tool, not resolved in this pass) | 0 | No |

**This is expected and honest, not a failure to report:** the edits made in
this pass are BOM/procurement-documentation-level and a metadata-only
schematic annotation. They correct *what real part should be sourced* and
*why the current placeholder is wrong* — they do not (and, per the
"zero-deviation" layout lock, should not without owner review) change
symbol-library associations, footprints, or net connectivity. Fixing the 82
`lib_footprint_mismatch` / 164 ERC violations for real requires either (a)
owner-approved footprint/symbol swaps, which is a real engineering decision
with layout consequences, or (b) real net-level schematic capture once the
NCE ball map is IP-owner-approved (not before). Neither is safe to do
unilaterally.

## Sources cited for real parts

- HMC8410LP2FE real datasheet (confirms mismatch): [Analog Devices product page](https://www.analog.com/en/products/hmc8410lp2fe.html)
- Renesas HXT45430 replacement: [Renesas product page](https://www.renesas.com/en/products/hxt45430)
- Samsung HBM3E Shinebolt KHBBC4B03B-MC1J: [Samsung Semiconductor China product page](https://semiconductor.samsung.cn/dram/hbm/hbm3e-shinebolt/khbbc4b03b-mc1j/)
- HBM4 allocation reality (why HBM4 itself isn't purchasable near-term): [TrendForce, 2026-01-09](https://www.trendforce.com/news/2026/01/09/news-nvidia-demand-fuels-hbm4-race-12-layer-ramps-16-layer-push-by-sk-hynix-samsung-and-micron/)
- HyperLight as TFLN sourcing target: [optics.org](https://optics.org/news/hyperlight-looks-to-scale-thin-film-lithium-niobate-with-taiwan-foundries)

## Still blocked / pending (unchanged from before this pass)

- **NCE BGA-2500 ball map remains DRAFT.** Per the standing rule, no agent
  may mark it final. It requires Bola Olatunji's (the IP owner's) review
  and correction before it can inform any real schematic net.
- **TFLN real pad map** for U3 is still pending HyperLight (or fallback
  vendor) engagement — not invented.
- **HBM3E footprint/package co-design** against the current placeholder
  footprint is not verified — needs a real datasheet-driven footprint swap,
  which is an owner decision given the layout lock.
- **HMC8410 -> HXT45430 footprint/package co-design** is likewise not
  verified or executed at the PCB level — same reasoning.
- **164 ERC / 82 DRC violations are unchanged** — see table above for why,
  and what would actually be required to move them.
- **214 schematic-parity figure from the repo's prior documentation is
  still not reproduced** by a live `kicad-cli pcb drc` run in this
  environment — needs investigation into whether a different kicad-cli
  invocation, report mode, or the referenced
  `after_non_netlist_fab_readiness_drc.json` in `07_engineering_reconcile/`
  used a different tool/version.

## Bottom line

**The board is NOT fab-ready.** This pass corrected sourcing documentation
with real, cited parts and added a systematic draft ball-map scaffold for
review — it did not (and could not, without owner-approved layout changes)
resolve the underlying ERC/DRC violations or produce a real, final ball map.
